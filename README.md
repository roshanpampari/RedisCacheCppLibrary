# RedisCacheCppLibrary
A lightweight Redis-inspired in-memory cache library in C++ with TTL-based key expiration.

## Features

- Set and get key-value pairs
- Optional TTL expiration
- Background cleanup thread for expired keys
- Thread-safe access using mutexes
- Can be built as a reusable C++ library
